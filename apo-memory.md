# APO seat memory

(The seat appends dated entries here: the Principal's corrections and preferences verbatim with the why, adopted conventions, decisions with reasoning, and standing artifact URLs. One entry per fact; strike what turns out wrong. This file is what makes the APO the same APO across every chat.)
